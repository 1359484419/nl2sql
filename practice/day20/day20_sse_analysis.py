"""
Day 20 — 练习 20-2：SSE vs WebSocket vs 普通请求
"""

print("=" * 55)
print("  SSE / WebSocket / 普通请求 对比")
print("=" * 55)

print("""
┌─────────────────┬────────────┬──────────────┬─────────────────┐
│                 │  普通请求   │     SSE      │   WebSocket     │
├─────────────────┼────────────┼──────────────┼─────────────────┤
│ 方向            │  单向       │  单向（服务→ │  双向            │
│                 │ (客户端→    │  客户端)     │  (都能发)       │
│                 │  服务端)    │              │                 │
├─────────────────┼────────────┼──────────────┼─────────────────┤
│ 谁发起          │  客户端     │  服务端       │  双方都可以     │
│                 │             │  (推送)      │                 │
├─────────────────┼────────────┼──────────────┼─────────────────┤
│ 连接方式        │  短连接     │  长连接       │  长连接         │
│                 │ (一次请求   │ (一次建立，   │ (双向通道)     │
│                 │  一次响应)  │  持续推送)    │                 │
├─────────────────┼────────────┼──────────────┼─────────────────┤
│ 使用场景        │  普通API    │  NL2SQL/聊天 │  游戏/实时     │
│                 │             │  推送/通知   │  协作           │
├─────────────────┼────────────┼──────────────┼─────────────────┤
│ 简单程度        │  最简单      │  简单        │  复杂           │
└─────────────────┴────────────┴──────────────┴─────────────────┘
""")

print("""
\nSSE 在 NL2SQL 项目里的使用：

NL2SQL 用 SSE 做流式输出：
- 用户提问 → 后端边生成 SQL 边推送 → 前端边显示
- 不用等 10 秒看到完整结果，而是每秒看到进度

yield 在 async 函数里：
- 普通函数：return 后结束
- async 函数：yield 可以"暂停"，返回值后继续执行
- SSE 用 yield 持续产出数据，HTTP 连接保持打开
""")
