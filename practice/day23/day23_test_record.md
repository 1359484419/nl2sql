# Day 23 — BM25 混合检索测试记录

## 修改内容

**修改文件：** `backend/rag/retriever.py`

**新增内容：**
1. `rank_bm25` 导入
2. `BM25Okapi` 索引初始化
3. `_ensure_bm25()` 方法
4. `search()` 方法里的 BM25 检索 + RRF 融合

## 测试对比

**问题：** "各部门用户平均使用时长"

| 检索方式 | 返回的表 | 效果 |
|---------|---------|------|
| 纯向量 | ? | ? |
| 纯 BM25 | ? | ? |
| RRF 混合 | ? | ? |

**问题：** "fact_workspace 表的 org_id 字段"

| 检索方式 | 返回的表 | 效果 |
|---------|---------|------|
| 纯向量 | ? | ? |
| 纯 BM25 | ? | ? |
| RRF 混合 | ? | ? |

## 结论

（写下你的结论：混合检索是否比单一路更好）
